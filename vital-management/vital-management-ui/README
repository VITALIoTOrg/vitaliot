Installation:

Need to have npm, bower installed.
Then you run
> npm install
> bower install
> grunt build

- If all goes well, the target/build folder will contain the application

> grunt dist
- Will produce the minified version of the application in target/dist folder

